spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: eureka-user
      password: passEureka24
server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

